package com.touchetime.presentation.state

import com.touchetime.utils.Constants

enum class TimeState(val value: Long) {
    TEN_SECONDS(Constants.TEN_SECONDS),
    ONE_MINUTES(Constants.ONE_MINUTE)
}
